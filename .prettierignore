# Lockfiles — managed by their package manager, not Prettier
pnpm-lock.yaml
package-lock.json
yarn.lock

# Build / cache outputs
dist/
build/
site/
coverage/
.cache/
.tsbuildinfo

# Per-package outputs (pnpm workspaces)
packages/**/dist/
packages/**/build/
examples/**/dist/

# Node tooling that has its own format
node_modules/

# Generated docs — produced byte-for-byte by `pnpm docs:tools` and guarded by a sync test;
# Prettier must not reformat it or it would fight the generator.
TOOL-REFERENCE.md
