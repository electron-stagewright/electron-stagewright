# Third-party notices

## axe-core

This package loads and injects the installed `axe-core` runtime to audit the
currently selected Electron renderer surface. `axe-core` is distributed under
the Mozilla Public License, version 2.0 (MPL-2.0). Its source, license text,
and third-party notices are provided by the installed `axe-core` npm package.

Electron Stagewright does not modify or bundle axe-core into this package; npm
installs it as a runtime dependency beside this plugin.
